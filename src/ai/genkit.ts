// Genkit is temporarily disabled to resolve dependency issues.
// To re-enable, run: npm install genkit @genkit-ai/google-genai
// Then, uncomment the original code.

export const ai = null;

/*
import {genkit} from 'genkit';
import {googleAI} from '@genkit-ai/google-genai';

export const ai = genkit({
  plugins: [googleAI()],
  model: 'googleai/gemini-2.5-flash',
});
*/
